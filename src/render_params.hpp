#pragma once
struct RenderParams{
    //PARAMETERS
    const int FPS = 60;
    const int TIME_SECONDS = 60;
    





    //DO not modify
    const int MAX_FRAMES = FPS * TIME_SECONDS;
    int frameCounter = 0;
};